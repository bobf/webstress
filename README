HTTP Stress Testing Client

Development
===========

Makefile assumes virtualenv installed in current directory, i.e. `bin/python`
should exist.

Install libs:

    `bin/pip install setup.py`

To run server:

    make run-web
        or
    make run-web-daemonize

To run tests:

    make test
